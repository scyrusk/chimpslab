<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CHIMPS Lab</title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <!-- Le styles -->
    <%= stylesheet_link_tag "application", :media => "all" %>

    <!-- Le fav and touch icons -->
    <link href="images/favicon.ico" rel="shortcut icon">
  </head>
  <body>

    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
          <div class="container">
          <a class="brand" href="/">CHIMPS Lab</a>
            <ul class="nav pull-right">
              <li><%= link_to("<i class=\"icon-home\"></i>Home".html_safe, root_path) %></li>
              <li><%= link_to "<i class=\"icon-book\"></i>Blog".html_safe, posts_path  %></li>
                <li><%= link_to "<i class=\"icon-user\"></i>People".html_safe, people_path  %></li>
                <li><%= link_to "<i class=\"icon-list-alt\"></i>Publications".html_safe, publications_path  %></li>
            </ul>
        </div>
      </div>
    </div>

    <div class="container">
      <div class="content">
        <div class="row">
           <div class="span8">
              <%= yield %>
            </div>
            <div class="span4">
              <div class="well sidebar-nav">
                <%= yield :sb %>
              </div><!--/.well -->
            </div><!--/span-->
        </div><!--/row-->
      </div><!--/content-->

      <div class="row">
        <div class="span12 bordered">
          <% if authorized? %>
            <div class="btn-group">
              <%= link_to 'Logout', logout_path, :class => 'btn btn-warning pull-left' %>
              <%= link_to 'Pages', pages_path, :class => 'btn btn-warning pull-left' %>
              <%= link_to 'Venues', venues_path, :class => 'btn btn-warning pull-left' %>
            </div>
          <% else %>
            <%= link_to 'Login', login_path, :class => 'btn btn-warning pull-left' %>
          <% end %>
          <p class="pull-right">&copy; CHIMPS Lab 2012</p>
        </div>
      </div>

    </div> <!-- /container -->

    <!-- Le javascript
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>
    <%= yield :js %>

  </body>
</html>
